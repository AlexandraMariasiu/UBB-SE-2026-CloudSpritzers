<ContentDialog
    x:Class="CloudSpritzers1.src.view.general.YouSure"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:CloudSpritzers1.src.viewModel.general" 
    Title="{x:Bind ViewModel.Title, Mode=OneWay}"
    PrimaryButtonText="{x:Bind ViewModel.ConfirmButtonText, Mode=OneWay}"
    CloseButtonText="{x:Bind ViewModel.CancelButtonText, Mode=OneWay}"
    DefaultButton="Primary">

    <StackPanel VerticalAlignment="Stretch">
        <TextBlock Text="{x:Bind ViewModel.Message, Mode=OneWay}" TextWrapping="Wrap" />
    </StackPanel>
</ContentDialog>